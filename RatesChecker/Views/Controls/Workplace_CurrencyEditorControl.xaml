<UserControl x:Class="RatesChecker.Views.Controls.Workplace_CurrencyEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RatesChecker.Views.Controls"
             xmlns:vm="clr-namespace:RatesChecker.ViewModels.WorkplaceControlsViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:CurrencyEditorControlViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Width="100" Height="25" Content="Open file" Margin="5" Command="{Binding OpenCurrencyFileCommand}">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource button_CommonRounded}">

                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="VerticalAlignment" Value="Top"/>
                    <Setter Property="Grid.RowSpan" Value="1"/>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsFileLoaded}" Value="False">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Grid.RowSpan" Value="2"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <DataGrid Grid.Row="1" Visibility="{Binding IsFileLoaded, Converter={StaticResource BoolToVisConverter}}" ItemsSource="{Binding LoadedCurrencyDynamicList}" Margin="5"/>
    </Grid>
</UserControl>