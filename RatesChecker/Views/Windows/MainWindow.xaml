<Window x:Class="RatesChecker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:RatesChecker.ViewModels"
        xmlns:controls="clr-namespace:RatesChecker.Views.Controls"
        xmlns:local="clr-namespace:RatesChecker"
        Title="{Binding WindowTitle}"
        mc:Ignorable="d" MinHeight="450" MinWidth="800" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <controls:SelectionBarControl Grid.Column="0"/>
        <controls:WorkplaceControl Grid.Column="1"/>
    </Grid>
</Window>
